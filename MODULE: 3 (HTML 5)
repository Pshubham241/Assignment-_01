Q-1:-What are the new tags added in HTML5? 
ans:-Elements introduced in HTML5
    1-audio
    2-canvas
    3-command
    4-embed
    5-svg
    6-nav
    7-video
    8-header
    9-footer
    10-section

 Q-02:-How to embed audio and video in a webpage?
 ans:-The HTML5 <audio> and <video> tags make it simple to add media to a website. You need to set src attribute to identify the media source and include a controls attribute so the user can play and pause the media.
 All you need to do is upload the file and receive an HTML  embed code.
 An easy way to embed audio on a website is by using a sound hosting site, such as SoundCloud or Mixcloud. 

 Q-03:-Semantic element in HTML5?
 ans:-  1-<article>
        2-<footer>
        3-<nav>
        4-<main>
        5-<section>
        6-<footer>
        7-<aside>


 Q-04:-Canvas and SVG tags 
 ans:-  (1)canvas-.The HTML <canvas> element is used to draw graphics, via JavaScript.
                   The<canvas>element is a container for graphics
                  .Canvas has poor scalability. Hence it is not suitable for printing on higher resolution
                  .Canvas gives better performance with smaller surface or larger number of objects.
                  .Canvas can be modified through script only
                  .Canvas is raster based and composed of pixel.

       (2)SVG-    .SVG has better scalability. So it can be printed with high quality at any resolution
                  .SVG gives better performance with smaller number of objects or larger surface.
                  .SVG can be modified through script and CSS
                  .SVG is vector based and composed of shapes
                  . SVG and useful for defining graphics such as boxes, circles, text, etc. SVG stands  forScalable Vector Graphics.
                  .The HTML <svg> element is a container for SVG graphics. SVG stands for Scalable Vector Graphics. 
